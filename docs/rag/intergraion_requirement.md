# C4ang 챗봇 - 기능별 연동 요구사항

> **목적**: 챗봇이 Internal API와 연동할 때 필요한 데이터 정보를 정리합니다.  
> 이 문서를 기반으로 Internal API 명세를 제공해주시면 연동 작업을 진행하겠습니다.

---

## 🎯 기능 1: 향수 검색/추천

### 📌 용도
사용자가 "상큼한 여름 향수 추천해줘"와 같이 자연어로 요청할 때 적합한 향수를 검색하고 추천합니다.

### 💬 사용 예시
- "플로랄 향수 추천해줘"
- "데이트할 때 뿌리기 좋은 향수 있어?"
- "여름에 어울리는 상큼한 향수 찾아줘"
- "10만원 이하 남성 향수 추천"

### 📥 챗봇이 보낼 수 있는 입력 (Input)

| 파라미터 | 타입 | 필수 | 설명 | 예시 값 |
|---------|------|------|------|--------|
| `query` | string | ✅ | 검색 쿼리 (자연어) | `"상큼한 여름 향수"` |
| `scent_family` | string | ❌ | 향조 필터 | `플로랄`, `우디`, `시트러스`, `오리엔탈`, `프레시`, `스파이시` |
| `occasion` | string | ❌ | 사용 상황 | `데이트`, `출근`, `캐주얼`, `파티`, `일상` |
| `season` | string | ❌ | 계절 | `봄`, `여름`, `가을`, `겨울` |
| `brand` | string | ❌ | 브랜드 | `디올`, `샤넬`, `조말론` 등 |
| `gender` | string | ❌ | 성별 | `남성`, `여성`, `유니섹스` |
| `price_min` | number | ❌ | 최소 가격 | `50000` |
| `price_max` | number | ❌ | 최대 가격 | `200000` |
| `top_k` | integer | ❌ | 결과 개수 | `5` (기본값) |

### 📤 챗봇이 필요한 출력 (Output)

```json
{
  "results": [
    {
      "id": "perfume_001",           // 필수: 향수 고유 ID
      "brand": "조말론",              // 필수: 브랜드명
      "name": "라임 바질 앤 만다린",   // 필수: 향수명
      "concentration": "EDC",        // 선택: 농도 (Parfum/EDP/EDT/EDC)
      "main_accords": "시트러스, 아로마틱",  // 선택: 주요 향조
      "description": "상쾌한 시트러스...",   // 선택: 설명
      "price": 195000,               // 선택: 가격
      "similarity_score": 0.95       // 선택: 유사도 점수 (0~1)
    }
  ],
  "total_count": 10
}
```

---

## 🎯 기능 2: 향수 상세 조회

### 📌 용도
특정 향수의 상세 정보(노트, 설명 등)를 조회합니다.

### 📥 챗봇이 보낼 수 있는 입력 (Input)

| 파라미터 | 타입 | 필수 | 설명 | 예시 값 |
|---------|------|------|------|--------|
| `product_id` | string | ✅ | 향수 고유 ID | `"perfume_001"` |

### 📤 챗봇이 필요한 출력 (Output)

```json
{
  "id": "perfume_001",
  "brand": "디올",
  "name": "미스 디올",
  "concentration": "EDP",
  "main_accords": "플로랄, 로즈",
  "top_notes": "만다린, 베르가못",       // 필수: 탑노트
  "middle_notes": "로즈, 피오니",        // 필수: 미들노트
  "base_notes": "머스크, 샌달우드",      // 필수: 베이스노트
  "description": "우아한 플로랄 향수...",
  "price": 180000,
  "image_url": "https://...",           // 선택: 이미지
  "detail_url": "https://..."           // 선택: 상세 페이지 링크
}
```

---

## 🎯 기능 3: 유사 향수 검색

### 📌 용도
특정 향수와 비슷한 향수를 찾습니다. (KNN 유사도 탐색)

### 📥 챗봇이 보낼 수 있는 입력 (Input)

| 파라미터 | 타입 | 필수 | 설명 | 예시 값 |
|---------|------|------|------|--------|
| `product_id` | string | ✅ | 기준 향수 ID | `"perfume_001"` |
| `top_k` | integer | ❌ | 결과 개수 | `3` (기본값) |

### 📤 챗봇이 필요한 출력 (Output)

```json
{
  "base_product_id": "perfume_001",
  "similar_products": [
    {
      "id": "perfume_002",
      "brand": "조말론",
      "name": "피오니 앤 블러쉬 스웨이드",
      "main_accords": "플로랄",
      "price": 210000,
      "similarity_score": 0.89
    }
  ]
}
```

---

## 🎯 기능 4: 향수 비교

### 📌 용도
2~4개의 향수를 비교 분석합니다.

### 📥 챗봇이 보낼 수 있는 입력 (Input)

| 파라미터 | 타입 | 필수 | 설명 | 예시 값 |
|---------|------|------|------|--------|
| `product_ids` | array[string] | ✅ | 비교할 향수 ID 목록 (2~4개) | `["perfume_001", "perfume_002"]` |

### 📤 챗봇이 필요한 출력 (Output)

```json
{
  "comparison_count": 2,
  "products": [
    {
      "id": "perfume_001",
      "brand": "디올",
      "name": "미스 디올",
      "concentration": "EDP",
      "main_accords": "플로랄, 로즈",
      "top_notes": "만다린",
      "middle_notes": "로즈",
      "base_notes": "머스크",
      "price": 180000
    },
    {
      "id": "perfume_002",
      "brand": "샤넬",
      "name": "코코 마드모아젤",
      "concentration": "EDP",
      "main_accords": "플로랄, 시트러스",
      "top_notes": "오렌지",
      "middle_notes": "재스민",
      "base_notes": "바닐라",
      "price": 220000
    }
  ]
}
```

---

## 📊 필요한 향수 데이터 필드 정리

| 필드명 | 타입 | 필수 | 설명 | 사용하는 기능 |
|--------|------|------|------|--------------|
| `id` | string | ✅ | 향수 고유 ID | 전체 |
| `brand` | string | ✅ | 브랜드명 | 전체 |
| `name` | string | ✅ | 향수명 | 전체 |
| `concentration` | string | ⭕ | 농도 (Parfum/EDP/EDT/EDC) | 검색, 상세, 비교 |
| `main_accords` | string | ⭕ | 주요 향조 | 검색, 상세, 비교 |
| `top_notes` | string | ⭕ | 탑노트 | 상세, 비교 |
| `middle_notes` | string | ⭕ | 미들노트 | 상세, 비교 |
| `base_notes` | string | ⭕ | 베이스노트 | 상세, 비교 |
| `description` | string | ❌ | 향수 설명 | 검색, 상세 |
| `price` | number | ❌ | 가격 | 전체 |
| `gender` | string | ❌ | 성별 타겟 | 검색 필터 |
| `image_url` | string | ❌ | 이미지 URL | 상세 |
| `detail_url` | string | ❌ | 상세 페이지 URL | 상세 |

> ✅ 필수 / ⭕ 권장 / ❌ 선택

---

## 🔍 필터 옵션 정리

### 향조 (scent_family)
- `플로랄`, `우디`, `시트러스`, `오리엔탈`, `프레시`, `스파이시`, `아로마틱`, `머스크`, `파우더리`

### 사용 상황 (occasion)
- `데이트`, `출근`, `캐주얼`, `파티`, `일상`, `특별한 날`

### 계절 (season)
- `봄`, `여름`, `가을`, `겨울`, `사계절`

### 성별 (gender)
- `남성`, `여성`, `유니섹스`

### 농도 (concentration)
- `Parfum` (가장 진함)
- `EDP` (Eau de Parfum)
- `EDT` (Eau de Toilette)
- `EDC` (Eau de Cologne)

---

## ❓ 확인이 필요한 사항

1. **향수 ID 형식**: Internal API에서 사용하는 향수 ID 형식이 어떻게 되나요?
2. **벡터 검색 지원 여부**: 텍스트 쿼리를 벡터로 변환해서 검색하는 기능이 있나요? 아니면 키워드 기반 검색인가요?
3. **필터 조합**: 여러 필터를 동시에 적용할 수 있나요? (예: 브랜드 + 향조 + 가격대)
4. **페이지네이션**: 검색 결과가 많을 경우 페이지네이션이 필요한가요?
5. **이미지 URL**: 향수 이미지 URL을 제공받을 수 있나요?

---

## 📞 연락처

이 문서를 기반으로 Internal API 명세를 공유해주시면 연동 작업을 진행하겠습니다!

